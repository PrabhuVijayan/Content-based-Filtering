# Content-based-Filtering
Content-based movie recommender using a two-tower NN. Learns user/movie embeddings to predict ratings and generate top-K recommendations. Includes data preprocessing, training pipeline, evaluation (RMSE, Precision@K, Recall@K, NDCG), and support for new/existing users.
